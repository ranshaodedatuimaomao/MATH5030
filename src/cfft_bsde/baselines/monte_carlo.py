"""Monte Carlo baselines (e.g. Milstein) — placeholder."""

from __future__ import annotations

from typing import Any, Mapping


def estimate_price(*, config: Mapping[str, Any] | None = None) -> float:
    """Placeholder: MC price for comparison."""
    raise NotImplementedError("Monte Carlo baseline")
