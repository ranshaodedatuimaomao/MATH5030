"""Accuracy vs wall-clock time (placeholder)."""

from __future__ import annotations

from typing import Any, Mapping


def run_study(*, config: Mapping[str, Any] | None = None) -> dict[str, Any]:
    """Placeholder: sweep solver settings and record errors + timings."""
    raise NotImplementedError("accuracy vs runtime benchmark")
