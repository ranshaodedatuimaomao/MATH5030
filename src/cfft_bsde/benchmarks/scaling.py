"""Scaling with grid size and time steps (placeholder)."""

from __future__ import annotations

from typing import Any, Mapping


def run_study(*, config: Mapping[str, Any] | None = None) -> dict[str, Any]:
    """Placeholder: complexity / empirical scaling curves."""
    raise NotImplementedError("scaling benchmark")
